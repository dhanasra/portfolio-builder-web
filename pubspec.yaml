name: portfolio_builder_ai
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Firebase
  firebase_core: ^2.15.1
  firebase_auth: ^4.8.0
  firebase_storage: ^11.2.6
  firebase_analytics: ^10.4.5
  firebase_crashlytics: ^3.3.5
  cloud_firestore: ^4.9.0

  # Routing
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
